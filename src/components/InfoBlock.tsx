import React, { FC } from "react";
import styled from "styled-components";

const StyledInfoBlock = styled.h1`
color
`;

const InfoBlock: FC = () => {
  return <div></div>;
};

export default InfoBlock;
